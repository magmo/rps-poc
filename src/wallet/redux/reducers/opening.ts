import { WalletState, OpeningState } from '../../states';
import { WalletAction } from '../actions';

export const openingReducer = (state: OpeningState, action: WalletAction): WalletState => {
  return state;
};
