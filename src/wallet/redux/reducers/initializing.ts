import { WalletState, InitializingState } from '../../states';
import { WalletAction } from '../actions';

export const initializingReducer = (state: InitializingState, action: WalletAction): WalletState => {
  return state;
};
